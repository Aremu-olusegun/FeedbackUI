import React from 'react'

const Header = () => {
    let text = "Feedback UI"
    return (
        <div className='header'>
            <h2 className='header-text'>{text}</h2>
        </div>
    )
}

export default Header
